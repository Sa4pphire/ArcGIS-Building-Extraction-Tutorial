# 完整项目结果与验证口径

仓库演示数据来自完整项目中的一个小裁块。以下结果来自完整数据处理与UNet训练，用于展示教程流程在真实规模上的结果。

## 数据生产

| 影像与标注 | 有效样本 |
|---|---:|
| 1.tif + MyProject2/sf.shp | 1,237对 |
| 2.tif + MyProject3/sf.shp | 696对 |
| 3.tif + MyProject3/sf2.shp | 556对 |
| 合计 | 2,489对 |

每组数据均检查：

- 影像和标签stem完全一致；
- 没有空标签和损坏文件；
- 尺寸均为512×512；
- 影像为RGB JPEG；
- 标签为单通道无损PNG；
- 标签值仅为0和255。

## UNet训练

当前正式UNet使用共2,489对样本：

- 训练集：2489；
- 验证集：1553；
- 空间缓冲：27；
- 模型：UNet；
- 训练轮数：30；
- 最佳轮次：12；
- 最佳验证集mIoU：0.7233；
- 建筑物IoU：约0.6795；
- 建筑物F1：约0.8092；
- 总体像素准确率：约0.8441。

指标来自最佳检查点的混淆矩阵和PaddleRS验证记录。第30轮mIoU为0.7044，因此使用最佳模型，而不是机械地使用最后一轮。

## 目标检测状态

完整目标检测数据准备和审计已经完成，PP-YOLO Tiny训练入口也已准备；但当前兼容训练环境不可用，本轮冒烟训练尚未完成。此前正式训练曾在第一轮中途停止，因此本仓库不发布检测模型或检测精度。
